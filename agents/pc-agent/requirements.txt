# RUDRA PC agent dependencies
aiomqtt>=2.0          # async MQTT client

# Optional, platform-specific (only needed for those features):
# pycaw ; sys_platform == "win32"      # Windows volume control
# comtypes ; sys_platform == "win32"   # required by pycaw
# (Linux volume uses pactl/amixer; macOS uses osascript — no pip deps.)
